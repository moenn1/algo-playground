{
  "name": "@tracedeck/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix ../../packages/trace-core run build && npm --prefix ../../packages/execution-engine run build && vite",
    "build": "npm --prefix ../../packages/trace-core run build && npm --prefix ../../packages/execution-engine run build && vite build",
    "preview": "vite preview",
    "test": "npm --prefix ../../packages/trace-core run build && npm --prefix ../../packages/execution-engine run build && vitest run --passWithNoTests",
    "typecheck": "npm --prefix ../../packages/trace-core run build && npm --prefix ../../packages/execution-engine run build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@tracedeck/execution-engine": "0.1.0",
    "@tracedeck/trace-core": "0.1.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
