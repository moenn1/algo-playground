{
  "name": "tracedeck",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "npm run verify:workspaces",
    "predev": "npm run verify:workspaces",
    "dev": "node scripts/dev.mjs",
    "predev:demo": "npm run verify:workspaces",
    "dev:demo": "node scripts/dev.mjs --seed-demo",
    "predev:web": "npm run verify:workspaces",
    "dev:web": "npm run dev --workspace @tracedeck/web",
    "predev:api": "npm run verify:workspaces",
    "dev:api": "npm run dev --workspace @tracedeck/api",
    "predemo:seed": "npm run verify:workspaces",
    "demo:seed": "npm run build --workspace @tracedeck/trace-core && npm run build --workspace @tracedeck/execution-engine && tsx scripts/demo-data.ts seed",
    "predemo:summary": "npm run verify:workspaces",
    "demo:summary": "npm run build --workspace @tracedeck/trace-core && npm run build --workspace @tracedeck/execution-engine && tsx scripts/demo-data.ts summary",
    "prebuild": "npm run verify:workspaces",
    "build": "npm run build --workspace @tracedeck/trace-core && npm run build --workspace @tracedeck/execution-engine && npm run build --workspace @tracedeck/api && npm run build --workspace @tracedeck/web",
    "verify:workspaces": "node scripts/check-workspace-links.mjs",
    "lint": "eslint .",
    "pretypecheck": "npm run verify:workspaces",
    "typecheck": "npm run typecheck --workspace @tracedeck/trace-core && npm run typecheck --workspace @tracedeck/execution-engine && npm run typecheck --workspace @tracedeck/api && npm run typecheck --workspace @tracedeck/web",
    "pretest": "npm run verify:workspaces",
    "test": "npm run test --workspace @tracedeck/trace-core && npm run test --workspace @tracedeck/execution-engine && npm run test --workspace @tracedeck/api && npm run test --workspace @tracedeck/web",
    "check": "npm run verify:workspaces && npm run lint && npm run typecheck && npm run test",
    "smoke": "npm run check && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@types/node": "^22.10.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "eslint": "^9.15.0",
    "globals": "^15.12.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.13.0",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
